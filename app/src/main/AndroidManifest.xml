<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalnode.glc22.feedr">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.digitalnode.glc22.feedr.REDDIT_USERNAME"
            android:value="TheFiveHundred" />
        <meta-data
            android:name="com.digitalnode.glc22.feedr.CLIENT_ID"
            android:value="yFjlO_gh1e2uBw" />
        <meta-data
            android:name="com.digitalnode.glc22.feedr.REDIRECT_URL"
            android:value="https://github.com/HalfMillennium/Feedr" />

        <activity android:name=".AccountsActivity" />
        <activity android:name=".logins.reddit.RedditLoginActivity"></activity>
    </application>

</manifest>